Exporting models from blender:
1. Select meshes to export
2. File -> Export -> Wavefront (.obj)
3. Limit to Selection only, Y forward, Z up.

To support vertex colors and texture attributes - use Fbx.
To have relative paths to the Textures - set Path Mode to Relative in the export dialogue.